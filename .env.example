# ============================================
# Environment Configuration for FocusFlow
# ============================================
# IMPORTANT: Add .env to .gitignore - NEVER commit secrets!

# ============================================
# DATABASE CONFIGURATION
# ============================================
# SQL Server SA password - CHANGE THIS!
# Requirements: Min 8 chars, uppercase, lowercase, numbers, symbols
DB_PASSWORD=YourStrong@Passw0rd
DB_NAME=FocusFlowDb

# ============================================
# JWT TOKEN CONFIGURATION
# ============================================
# Secret key for signing JWT tokens - MUST be 32+ characters
# Generate secure secret: openssl rand -base64 32
JWT_SECRET=YourSuperSecretKeyThatIsAtLeast32CharactersLong!
JWT_ISSUER=FocusFlowApi
JWT_AUDIENCE=FocusFlowClient
JWT_EXPIRATION_MINUTES=60

# ============================================
# APPLICATION URLS
# ============================================
# API base URL (used by client to connect)
API_URL=http://localhost:5094

# Client URL (used for CORS configuration)
CLIENT_URL=http://localhost:3000

# ============================================
# PORTS
# ============================================
# Host machine ports (change if ports are already in use)
DATABASE_PORT=1433
API_PORT=5094
CLIENT_PORT=3000

# ============================================
# ENVIRONMENT
# ============================================
# Options: Development, Staging, Production
ASPNETCORE_ENVIRONMENT=Production

# ============================================
# PRODUCTION CONFIGURATION NOTES
# ============================================
# For production deployment:
# 1. Generate strong passwords: openssl rand -base64 32
# 2. Use secrets management: Azure Key Vault, AWS Secrets Manager, HashiCorp Vault
# 3. Enable HTTPS (see docker-compose.prod.yml example below)
# 4. Configure CORS with specific origins (not *)
# 5. Use managed database services instead of containerized DB
# 6. Enable monitoring and logging (Serilog, Application Insights, etc.)
